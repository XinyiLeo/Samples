Problems solved:
1.Read the strings 
     1.1 Use gets() dunction to get the strings.
     1.2 Count the number of the strings 
           When (int)(str[i])!= 0 , cnt++;
2. Compare the strings
     loop and if to judge.
          WHen all the strings are same, cnt++; 
  