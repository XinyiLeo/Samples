   Problem:
     Before the change of d.txt, I tried to sompare string com 
with the string scaned from the file.
   


What needs to be done:
Use a[4][j] to store 4 strings(4 people's information).
 Use  like fgets(fd,1000,a[1][j]); to get 4 lines.
  Use for (i=0;i<4;i++)
{
   j=rand()%4
   swap(i,j)

}
#define swap(ij) i>j ? i:j 
to get the random order
